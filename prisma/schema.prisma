// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Default Prisma client generator (prisma-client-js) → outputs to
// node_modules/.prisma/client. This is the location Vercel automatically
// bundles the query-engine binary for, avoiding "Query Engine not found" on
// serverless. binaryTargets pins the Vercel runtime (rhel-openssl-3.0.x)
// alongside the local platform. Regenerated via "postinstall": "prisma generate".

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Invitation {
  id           String   @id @default(uuid())
  slug         String   @unique
  locale       String
  templateId   String
  coupleNames  String
  eventDate    String
  eventTime    String
  venueName    String
  venueAddress String
  mapUrl       String?
  message      String?
  createdAt    DateTime @default(now())
}
